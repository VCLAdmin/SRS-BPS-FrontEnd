import { BpsUnifiedProblem } from './bps-unified-problem';

describe('BpsUnifiedProblem', () => {
  it('should create an instance', () => {
    expect(new BpsUnifiedProblem()).toBeTruthy();
  });
});
