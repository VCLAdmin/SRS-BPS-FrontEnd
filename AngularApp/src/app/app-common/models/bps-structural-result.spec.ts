import { StructuralResult } from './bps-structural-result';

describe('BpsStructuralResult', () => {
  it('should create an instance', () => {
    expect(new StructuralResult()).toBeTruthy();
  });
});
