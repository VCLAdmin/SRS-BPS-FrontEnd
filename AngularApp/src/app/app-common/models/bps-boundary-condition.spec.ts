import { BpsBoundaryCondition } from './bps-boundary-condition';

describe('BpsBoundaryCondition', () => {
  it('should create an instance', () => {
    expect(new BpsBoundaryCondition()).toBeTruthy();
  });
});
