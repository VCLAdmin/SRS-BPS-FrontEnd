import { BpsSimplifiedProblem } from './bps-simplified-problem';

describe('BpsSimplifiedProblem', () => {
  it('should create an instance', () => {
    expect(new BpsSimplifiedProblem()).toBeTruthy();
  });
});
