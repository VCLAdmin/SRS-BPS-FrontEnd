import { IframeExchangeData } from './iframe-exchange-data';

describe('IframeExchangeData', () => {
  it('should create an instance', () => {
    expect(new IframeExchangeData()).toBeTruthy();
  });
});
